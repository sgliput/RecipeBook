/*const addedFields = this.state.DirAddedFields + 1;
        const onChange = (event) => {        
            this.setState(DirChangeFunctions[addedFields](event, this.state));
        }
*/

const Step2Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step2 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step3Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step3 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step4Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step4 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step5Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step5 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step6Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step6 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step7Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step7 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step8Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step8 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step9Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step9 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step10Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step10 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step11Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step11 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step12Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step12 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step13Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step13 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step14Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step14 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step15Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step15 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step16Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step16 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step17Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step17 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step18Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step18 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step19Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step19 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const Step20Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step20 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step21Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step21 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step22Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step22 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step23Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step23 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step24Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step24 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step25Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step25 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step26Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step26 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step27Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step27 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step28Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step28 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step29Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step29 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};
const Step30Change = (event, state) => {
    const value = event.target.value;
    const steps = state.directions;
    steps.step30 = value;

    //Changes state.directions to the current steps object
    return {
        directions: steps
    };
};

const changeFunctions = [Step2Change, Step3Change, Step4Change, Step5Change, Step6Change, Step7Change, Step8Change, Step9Change, Step10Change, Step11Change, Step12Change, Step13Change, Step14Change, Step15Change, Step16Change, Step17Change, Step18Change, Step19Change, Step20Change, Step21Change, Step22Change, Step23Change, Step24Change, Step25Change, Step26Change, Step27Change, Step28Change, Step29Change, Step30Change];

export default changeFunctions;